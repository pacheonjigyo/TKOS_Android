<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/snake</item>
        <item name="colorPrimaryDark">@color/snake</item>
        <item name="colorAccent">@color/snake</item>
    </style>
    <style name="SplashTheme" parent="@android:style/Theme.Light.NoTitleBar"></style>
</resources>